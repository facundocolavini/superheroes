import React, { useState } from 'react'
import { Toast,Row,Col,Button,ToastContainer,Form} from 'react-bootstrap';

const Toasts = ({show}) => {
  const [view, setShow] = useState(true);

  const [position, setPosition] = useState('top-start');
    return (

        <>
        </>
    );
}

export default Toasts
